# =============================================================================
# TravelMaster AI Copilot 環境變數配置範例
# =============================================================================

# -----------------------------------------------------------------------------
# LLM 提供者選擇
# -----------------------------------------------------------------------------
LLM_PROVIDER=gemini  # gemini | siliconflow

# -----------------------------------------------------------------------------
# Google Gemini API
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash
GEMINI_DEFAULT_MAX_TOKENS=4000
GEMINI_GENERAL_MAX_TOKENS=1000
GEMINI_LEGAL_MAX_TOKENS=4000
GEMINI_MARKETING_MAX_TOKENS=10000

# -----------------------------------------------------------------------------
# SiliconFlow API (備用 LLM)
# -----------------------------------------------------------------------------
SILICONFLOW_API_KEY=your_siliconflow_key_here
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3
SILICONFLOW_MARKETING_MODEL=Qwen/Qwen2.5-32B-Instruct
SILICONFLOW_MARKETING_TEMPERATURE=0.7
SILICONFLOW_MARKETING_MAX_TOKENS=10000

# -----------------------------------------------------------------------------
# BFL/Flux 圖片生成 API
# -----------------------------------------------------------------------------
BFL_API_KEY=your_bfl_api_key_here
BFL_BASE_URL=https://api.bfl.ml/v1
BFL_MODEL=flux-pro
BFL_API_STYLE=auto

# -----------------------------------------------------------------------------
# RAG 檢索增強生成設定
# -----------------------------------------------------------------------------
RAG_MAX_CHARS=900
RAG_TOP_K=4

# -----------------------------------------------------------------------------
# Redis 快取設定
# -----------------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_ENABLED=true
REDIS_CACHE_TTL=3600  # 快取過期時間（秒），預設 1 小時

# -----------------------------------------------------------------------------
# Qdrant 向量資料庫設定
# -----------------------------------------------------------------------------
QDRANT_ENABLED=true
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=travelmaster_rules
QDRANT_API_KEY=  # 若使用 Qdrant Cloud，填入 API Key

# -----------------------------------------------------------------------------
# CORS 設定
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=

# -----------------------------------------------------------------------------
# 圖片生成非同步模式
# -----------------------------------------------------------------------------
IMAGE_GENERATION_ASYNC=true  # 是否使用非同步模式
IMAGE_GENERATION_TIMEOUT=60  # 非同步模式超時秒數

